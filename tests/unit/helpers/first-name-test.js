import {
  firstName
} from 'banbury/helpers/first-name';

module('FirstNameHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = firstName(42);
  ok(result);
});
